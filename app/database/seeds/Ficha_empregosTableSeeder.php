<?php

class Ficha_empregosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('ficha_empregos')->truncate();

		$ficha_empregos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('ficha_empregos')->insert($ficha_empregos);
	}

}

<?php

class Ficha_setorsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('ficha_setors')->truncate();

		$ficha_setors = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('ficha_setors')->insert($ficha_setors);
	}

}

<?php

class Ficha_cursosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('ficha_cursos')->truncate();

		$ficha_cursos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('ficha_cursos')->insert($ficha_cursos);
	}

}

<?php

class Funcionario_setorsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('funcionario_setors')->truncate();

		$funcionario_setors = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('funcionario_setors')->insert($funcionario_setors);
	}

}

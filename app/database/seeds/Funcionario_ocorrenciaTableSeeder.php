<?php

class Funcionario_ocorrenciaTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('funcionario_ocorrencia')->truncate();

		$funcionario_ocorrencia = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('funcionario_ocorrencia')->insert($funcionario_ocorrencia);
	}

}

<?php

class SetorsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('setors')->truncate();

		$setors = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('setors')->insert($setors);
	}

}

<table class="table table-striped table-bordered table-hover" id="dataTables-example" ng-controller="FuncionarioController">
    <thead>
        <tr>
            <th>#</th>
            <th>Cod. Interno</th>
            <th>Nome</th>
            <th>Estrutura</th>
            <th>Função</th>
            <th>Data Nascimento</th>
        </tr>
    </thead>
    <tbody>

        <tr class="odd gradeX" ng-repeat="(funcionarios in funcionario">
            <td>{{ funcionario.id }}</td>
            <td>{{ funcionario.cod_interno }}</td>
            <td>{{ funcionario.nome }}</td>
            <td>{{ funcionario.estrutura }}</td>
            <td>{{ funcionario.funcao }}</td>
            <td class="center">{{ funcionario.data_nascimento }}</td>
        </tr>

    </tbody>
</table>

<script>
    var FuncionarioCtrl = function($scope){
        $scope.funcionarios = <% $funcionarios %>;
    }
</script>